import classes from './ItemsSummary.module.css';

const ItemsSummary = () =>{
  return (
    <section className={classes.summary}>
      <h2>Games and wallet codes at cheaper price!</h2>
      <p>Buy your favorite games at reasonable price.</p>
      <p>Become a member to receive exclusive offers!</p>
    </section>
  );
};

export default ItemsSummary;